# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Environment
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.ai-settings.json

node_modules
*.local

# Build output — zawiera wkompilowane klucze klienta, nigdy nie trafia do repo
dist
dist-ssr

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Tauri — katalog budowania Rusta (gigabajty artefaktów)
src-tauri/target/
src-tauri/gen/schemas/

# Worktree'y agentów Claude Code — robocze kopie repo, nigdy nie trafiają do repo
.claude/worktrees/

# Eksport kont z Firebase Auth — hashe haseł i adresy kursantów.
# Domyślnie ląduje poza repo (iCloud Drive), ale gdyby ktoś zmienił
# CRIBRO_BACKUP_DIR na katalog projektu, to i tak nie wyjdzie na GitHub.
auth-users-*.json
