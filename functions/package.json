{
  "name": "cribro-functions",
  "description": "Powiadomienia e-mail o nowych pracach domowych",
  "private": true,
  "type": "commonjs",
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "deploy": "npm run build && PATH=\"/opt/homebrew/opt/node@22/bin:$PATH\" firebase deploy --only functions",
    "logs": "PATH=\"/opt/homebrew/opt/node@22/bin:$PATH\" firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^6.5.0"
  },
  "devDependencies": {
    "typescript": "^5.8.2"
  }
}
