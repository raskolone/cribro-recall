sed -i 's/<Button onClick={onBack}>{i18n.t("Wróć do listy testów")}<\/Button>/&\n        <Button \n          onClick={() => {\n            exportTestToPDF({\n              ...test,\n              studentAnswers: answers,\n              aiFeedback: gradingResult?.feedback,\n              score: gradingResult?.score,\n              completedAt: new Date().toISOString()\n            }, i18n.t)\n          }}\n          className="ml-4 bg-transparent border border-white\/20 text-white hover:bg-white\/10"\n        >\n          <Download className="w-4 h-4 mr-2 inline" \/>\n          {i18n.t("Pobierz raport (PDF)")}\n        <\/Button>/' components/tests/TakeTestScreen.tsx
sed -i '12i import { exportTestToPDF } from "../../utils/pdfExport";\nimport { Download } from "lucide-react";' components/tests/TakeTestScreen.tsx
